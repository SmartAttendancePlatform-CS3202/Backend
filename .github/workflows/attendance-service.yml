name: attendance-service

on:
  push:
    branches: [main]
    paths:
      - "services/attendance-service/**"
      - "libs/shared-core/**"
  pull_request:
    paths:
      - "services/attendance-service/**"
      - "libs/shared-core/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ./libs/shared-core
      - run: pip install -e ./services/attendance-service
      - run: pip install pytest
      - run: pytest services/attendance-service/tests

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        run: curl -X POST "${{ secrets.RENDER_ATTENDANCE_SERVICE_DEPLOY_HOOK }}"
