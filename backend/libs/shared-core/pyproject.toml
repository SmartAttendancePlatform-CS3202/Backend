[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-core"
version = "0.1.0"
description = "Shared utilities for the backend microservices"
requires-python = ">=3.11"
